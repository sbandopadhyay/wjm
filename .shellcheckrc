# ShellCheck configuration for WJM
# https://github.com/koalaman/shellcheck

# Default shell
shell=bash

# Disable specific checks that are intentional in WJM:
# SC2034: Variable appears unused (used via source)
# SC1090: Can't follow non-constant source (dynamic module loading)
# SC1091: Not following sourced file (external config)
disable=SC2034,SC1090,SC1091

# Enable optional checks
enable=all

# External sources - these are sourced dynamically
external-sources=true

# Severity threshold (error, warning, info, style)
severity=warning
